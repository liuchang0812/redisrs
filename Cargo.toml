[package]
name = "redisrs"
version = "0.1.0"
edition = "2021"

[dependencies]
rocksdb = "0.22"
tokio = { version = "1", features = ["full"] }
bytes = "1"
thiserror = "1"

[profile.release]
opt-level = 3
